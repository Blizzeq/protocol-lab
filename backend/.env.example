# === Baza danych (Supabase: projekt "pobieranie", schemat protocol_lab) ===
# Skopiuj DOKŁADNY connection string z dashboardu:
#   Supabase → (projekt pobieranie) → Connect → "Transaction pooler" / ORMs
# i zamień prefiks "postgresql://" na "postgresql+asyncpg://".
# Host poolera bywa różny (aws-0-... lub aws-1-...), dlatego skopiuj go 1:1 z dashboardu.
#
# Szablon (port 6543 = transaction pooler):
DATABASE_URL=postgresql+asyncpg://postgres.pfhmxiscodgkepxbavpv:[TWOJE-HASLO-DB]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# === Frontend / CORS ===
CORS_ORIGINS=http://localhost:3000

# === Środowisko ===
ENVIRONMENT=development

# === Dopinane w kolejnych milestone'ach ===
# REDIS_URL=redis://localhost:6379/0
# JWT_SECRET=zmien-mnie-na-dlugi-losowy-sekret
