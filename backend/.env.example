# === Database (Supabase: project "pobieranie", schema protocol_lab) ===
# Copy the EXACT connection string from the dashboard:
#   Supabase → (project pobieranie) → Connect → "Transaction pooler" / ORMs
# and replace the "postgresql://" prefix with "postgresql+asyncpg://".
# The pooler host varies (aws-0-... or aws-1-...), so copy it verbatim from the dashboard.
#
# Template (port 6543 = transaction pooler):
DATABASE_URL=postgresql+asyncpg://postgres.pfhmxiscodgkepxbavpv:[YOUR-DB-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# === Frontend / CORS ===
CORS_ORIGINS=http://localhost:3000

# === Environment ===
ENVIRONMENT=development

# === Added in upcoming milestones ===
# REDIS_URL=redis://localhost:6379/0
# JWT_SECRET=change-me-to-a-long-random-secret
